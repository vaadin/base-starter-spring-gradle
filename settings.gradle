pluginManagement {
  repositories {
      mavenLocal()
      maven { url = 'https://maven.vaadin.com/vaadin-prereleases' }
      gradlePluginPortal()
  }
  plugins {
      id 'com.vaadin.flow' version "${vaadinVersion}"
  }
}

include 'lib'
